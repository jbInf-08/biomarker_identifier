version: 2
updates:
  - package-ecosystem: "pip"
    directory: "/backend"
    schedule:
      interval: "monthly"
    open-pull-requests-limit: 3
    labels:
      - "dependencies"
      - "python"
    groups:
      backend-patch-minor:
        update-types:
          - "minor"
          - "patch"
    # Paused: numba (a transitive dep) requires numpy<2.5, so numpy 2.5+
    # cannot resolve -- pip backtracks numba into versions that predate
    # Python 3.12 and the build fails on "Cannot install on Python version
    # 3.12". Lift when numba supports numpy 2.5.
    ignore:
      - dependency-name: "numpy"
        versions: [">= 2.5"]

  - package-ecosystem: "npm"
    directory: "/frontend"
    schedule:
      interval: "monthly"
    open-pull-requests-limit: 3
    labels:
      - "dependencies"
      - "frontend"
    groups:
      frontend-patch-minor:
        update-types:
          - "minor"
          - "patch"
    # Paused until the Tailwind 4 migration is done deliberately: v4 replaces
    # tailwind.config.js with CSS-first @theme configuration and moves the
    # PostCSS integration to the separate @tailwindcss/postcss plugin, so as an
    # isolated bump it breaks the build. It also needs a visual check that the
    # rendered UI is unchanged, which CI cannot do. Remove when migrating.
    ignore:
      - dependency-name: "tailwindcss"
        versions: [">= 4"]

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
    open-pull-requests-limit: 2
    labels:
      - "dependencies"
      - "github-actions"
