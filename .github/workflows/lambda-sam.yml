# Optional: package backend + validate SAM template. Uncomment deploy job when AWS creds are ready.

name: Lambda SAM CI

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "backend/**"
      - "deployment/serverless/**"
      - ".github/workflows/lambda-sam.yml"

jobs:
  test-and-validate:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      # requirements-prod.txt pins rpy2, which refuses to build in API mode
      # without R on PATH/R_HOME, and its rinterface link step needs libtirpc.
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y r-base r-base-dev libtirpc-dev

      - name: Install backend + Mangum (Lambda adapter)
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-prod.txt mangum

      - name: Smoke tests
        env:
          SECRET_KEY: ci-test-secret-key-not-for-production-use-32b
          DEBUG: "true"
        run: |
          pip install -r requirements-dev.txt
          pytest tests/integration/test_api_surface.py tests/unit/test_run_access.py -q --tb=short

      - name: Set up SAM CLI
        uses: aws-actions/setup-sam@v2
        with:
          version: "1.110.0"

      - name: SAM validate
        working-directory: deployment/serverless
        # sam validate parses the template locally and needs no credentials, but
        # the CLI still refuses to run without a region ("AWS Region was not
        # found"). Pass one explicitly; the commented-out deploy job below sets
        # its own region via configure-aws-credentials, so this does not clash.
        run: sam validate -t template.yaml --region us-east-1

      # --- Uncomment to deploy (set repo secrets: AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_REGION) ---
      # - name: Configure AWS credentials
      #   uses: aws-actions/configure-aws-credentials@v4
      #   with:
      #     aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
      #     aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      #     aws-region: ${{ secrets.AWS_REGION }}
      #
      # - name: SAM build and deploy
      #   working-directory: deployment/serverless
      #   run: |
      #     sam build -t template.yaml
      #     sam deploy --no-confirm-changeset --capabilities CAPABILITY_IAM
