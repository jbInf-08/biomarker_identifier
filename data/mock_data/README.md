# Deprecated: Do Not Use

This directory previously contained mock/synthetic data and is **not for use**.

- **No mock, fake, dummy, made-up, or artificial data** should be used in this project.
- For real data, use files under `data/external_sources/` (e.g. TCGA, ClinVar).
- Tests and pipelines use only real data; see `tests/run_all_tests.py` and `data/external_sources/`.
